name: sudanile_kitchen
description: Sudanile Kitchen - South Sudanese Culinary Heritage App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  cupertino_icons: ^1.0.8
  http: ^1.3.0
  provider: ^6.1.2
  shared_preferences: ^2.5.3
  flutter_rating_bar: ^4.0.1
  url_launcher: ^6.3.1
  intl: ^0.20.2
  infinite_scroll_pagination: ^4.0.0
  cached_network_image: ^3.3.0  # Add this
  video_player: ^2.8.0          # Add this

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/
